{
  "app": {
    "model": "s",
    "crontab_cmd": [
      {
        "crontab": "0/1 * * * * ?",
        "cmd": "ls -al"
      },
      {
        "crontab": "0/1 * * * * ?",
        "cmd": "php -v"
      }
    ]
  }
}